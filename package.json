{
  "name": "turbofan-js",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm-run-all clean build:js",
    "build:js": "npx esbuild ./src/js/index.js --bundle --minify --outfile=dist/turbofan.js",
    "clean": "rimraf ./dist",
    "dev": "npm-run-all clean --parallel dev:js",
    "dev:js": "npx esbuild ./src/js/index.js --bundle --sourcemap --watch --outfile=dist/turbofan.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.19.8",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.5"
  }
}
